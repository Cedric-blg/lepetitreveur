<form method="post" action="{{ path('app_child_delete', {'id': child.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet enfant?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ child.id) }}">
    <button class="btn">Supprimer</button>
</form>
