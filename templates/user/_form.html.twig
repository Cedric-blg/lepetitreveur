{{ form_start(form, { attr: { class: 'row g-3' } }) }}
    {{ form_widget(form) }}
    <div class="col-12">
        <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
